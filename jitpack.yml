jdk:
  - openjdk17
before_install:
  - sdk install java 17.0.12-oracle
  - sdk use java 17.0.12-oracle
install:
  - ./gradlew clean :library:assembleRelease :library:publishToMavenLocal
  - find . -name "*.aar"
  - ls -l ~/.m2/repository/com/github/SamNeill/candybar/